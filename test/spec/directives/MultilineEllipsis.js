'use strict';

describe('Directive: MultilineEllipsis', function () {

    // mock directive. ignore it.

//  beforeEach(module('lergoApp'));
//
//  var element;
//
//  it('should make hidden element visible', inject(function ($rootScope, $compile) {
//    element = angular.element('<div multiline-ellipsis text="this is some text"></div>');
//    element = $compile(element)($rootScope);
//
//      $rootScope.$digest();
//
//    waitsFor(function(){
//        console.log(element.text()); return element.text().indexOf('this is some text') >=0;}, 'Tag should compile', 2000);
//  }));
});
